function greeter(name) {
    console.log('hello' + name);
}
var user = 'Neslihan';
greeter(user);
//typescriptın en guzel tarafı typescrip dosyasının içerisine javascript yazmış bile olsak 
//gecerli bir typescrippt olmuş oluyor.